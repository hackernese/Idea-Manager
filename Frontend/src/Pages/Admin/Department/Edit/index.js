import styles from './style.module.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

function EditDepartment() {
    return (
        <div>
            <h1>Edit existing department here</h1>
        </div>
    );
}

export default EditDepartment;
